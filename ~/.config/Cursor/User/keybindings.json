[
    {
        "key": "ctrl+shift+f10",
        "command": "workbench.action.debug.runToCursor",
        "when": "debuggersAvailable && inDebugMode"
    },
    {
        "key": "ctrl+shift+f10",
        "command": "debug.runToCursor",
        "when": "debuggersAvailable && inDebugMode"
    },
    {
        "key": "ctrl+f10",
        "command": "extension.multiCommand.execute",
        "args": {
            "sequence": [
                "workbench.action.debug.runToCursor"
            ]
        },
        "when": "debuggersAvailable && inDebugMode"
    }
] 